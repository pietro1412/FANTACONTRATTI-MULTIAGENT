# === AMBIENTE TEST LOCALE ===
# PostgreSQL via Docker (docker-compose up db-test)
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/fantacontratti_test?schema=public"
DATABASE_URL_UNPOOLED="postgresql://postgres:postgres@localhost:5434/fantacontratti_test?schema=public"

# JWT Secrets (test only)
JWT_ACCESS_SECRET="test-access-secret-32-chars-minimum-ok"
JWT_REFRESH_SECRET="test-refresh-secret-32-chars-minimum-ok"

# API
API_PORT=3003
FRONTEND_URL="http://localhost:5173"

# App
VITE_APP_NAME="Fantacontratti"
VITE_APP_ENV="test"
VITE_API_URL="http://localhost:3003"

# Pusher (disabled in test - use mock)
PUSHER_APP_ID="test"
PUSHER_SECRET="test"
VITE_PUSHER_KEY="test"
VITE_PUSHER_CLUSTER="mt1"

# Email (disabled in test)
EMAIL_PROVIDER="gmail"
GMAIL_USER="test@test.com"
GMAIL_APP_PASSWORD="test"
